import { Routes } from '@angular/router';
import { ListUsersComponentComponent } from './list-users-component/list-users-component.component';

export const routes: Routes = [
    {path: "users", component:ListUsersComponentComponent},
];
